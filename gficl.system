(in-package "CL-USER")
(defvar *gficl-source-dir* "/7/gtk/EXT-LISP/gficl/")
(defvar *gficl-binary-dir* (binary-directory *gficl-source-dir*))

(mk:defsystem :gficl-examples
  :source-pathname *gficl-source-dir*
  :binary-pathname *gficl-binary-dir*
  :source-extension "lisp"
  :depends-on (:gficl
	       :gficl/load)
  :components ((:module "examples"
		:components ((:file "package")
			     (:file "minimum")
			     (:file "minimum-app")
			     (:file "quad-spin")
			     (:file "cube-wave")
			     (:file "post-processing")
			     (:file "model-loading")
			     (:file "font-rendering")
			     (:file "shadows")
			     (:file "pbr")))))

#+nil
(mk:oos :gficl-examples :load :compile-during-load t)

(mk:defsystem :gficl-examples-shadertoy
  :source-pathname *gficl-source-dir*
  :binary-pathname *gficl-binary-dir*
  :source-extension "lisp"
  :depends-on (:gficl-bt)
  :components ((:module "examples"
		:components ((:file "gficl-shadertoy-bt")))))

#+nil
(mk:oos :gficl-examples-shadertoy :load :compile-during-load t)

(mk:defsystem :gficl-texture-util
  :source-pathname *gficl-source-dir*
  :binary-pathname *gficl-binary-dir*
  :source-extension "lisp"
  :depends-on (:gficl :gficl/load/imlib2)
  :components ((:module "src/"
		:components ((:file "u-tex-info")))))

#+nil
(mk:oos :gficl-texture-util :load :compile-during-load t)

(mk:defsystem :gficl-examples-shadertoys-extra
  :source-pathname *gficl-source-dir*
  :binary-pathname *gficl-binary-dir*
  :source-extension "lisp"
  :depends-on (:gficl-examples-shadertoy :gficl-texture-util)
  :components ((:module "examples/extra/"
		:components ((:file "gficl-shadertoy-gtk4-demo")
			     (:file "gficl-shadertoy-tbs")))))

#+nil
(mk:oos :gficl-examples-shadertoys-extra :load :compile-during-load t)

(mk:defsystem :gficl/load
  :source-pathname *gficl-source-dir*
  :binary-pathname *gficl-binary-dir*
  :source-extension "lisp"
  :depends-on (:gficl
	       :cl-wavefront
	       :cl-gltf
	       :pngload
	       :cl-jpeg
	       :truetype-clx)
  :components ((:module "src/load"
		:components ((:file "package")
			     (:file "model")
			     (:file "shader")
			     (:file "image")
			     (:file "font")))))

#+nil
(mk:oos :gficl/load :load :compile-during-load t)

(mk:defsystem :gficl
  :source-pathname *gficl-source-dir*
  :binary-pathname *gficl-binary-dir*
  :source-extension "lisp"
  :depends-on (:uiop
	       :cl-glfw3
	       :cl-opengl
	       :cffi)
  :components ((:module "src"
		:components ((:file "package")
			     (:file "base-app"
			      :depends-on ("gficl" "types"))
			     (:file "fbo-mixin-app"
			      :depends-on ("base-app"))
			     (:file "gficl"
			      :depends-on ("input"))
			     (:file "input"
			      :depends-on ("state"))
			     (:file "state")
			     (:file "types")
			     (:file "vector")
			     (:file "quaternion"
			      :depends-on ("vector"))
			     (:file "matrix"
			      :depends-on ("quaternion"
					   "shader"))
			     (:file "shader"
			      :depends-on ("types"))
			     (:file "framebuffer"
			      :depends-on ("types"
					   "image"))
			     (:file "image"
			      :depends-on ("types"))
			     (:file "vertex"
			      :depends-on ("types"))
			     (:file "buffer"
			      :depends-on ("types"))))))

#+nil
(mk:oos :gficl :load :compile-during-load t)

(pushnew :lem-mailbox *features*)

(mk:defsystem :gficl-bt
  :source-pathname *gficl-source-dir*
  :binary-pathname *gficl-binary-dir*
  :source-extension "lisp"
  :depends-on (:gficl :bordeaux-threads (:feature :lem-mailbox :lem-mailbox))
  :components ((:module "src"
		:components ((:file "base-app-bt")))))

#+nil
(mk:oos :gficl-bt :load :compile-during-load t)

(mk:defsystem :gficl-examples-bt
  :source-pathname *gficl-source-dir*
  :binary-pathname *gficl-binary-dir*
  :source-extension "lisp"
  :depends-on (:gficl-bt)
  :components ((:module "examples"
		:components ((:file "minimum-app")
			     (:file "minimum-app-bt")
			     #+nil ;;madhu 260407 problems in lw,ccl
			     (:module "extra"
			      :components ((:file "immediate-mode-shim")))))))

#+nil
(mk:oos :gficl-examples-bt :load :compile-during-load t)


;;; ----------------------------------------------------------------------
;;;
;;; extra loaders
;;;

(mk:defsystem :gficl/load/imlib2
  :source-pathname *gficl-source-dir*
  :binary-pathname *gficl-binary-dir*
  :source-extension "lisp"
  :depends-on ( #+nil :gficl/load
		(:feature :claw-imlib
		 "claw-cxx-imlib2.bindings"))
  :components ((:module "src/load"
		:components (;; #+nil
			     (:file "imlib2-min-x86_64-pc-linux-gnu"
			      :if-feature (:not :claw-imlib))
			     (:file "image-imlib2")))))

#+nil
(mk:oos :gficl/load/imlib2 :load :compile-during-load t)

#+nil
(load "~/cl/extern/claw-cxx-ft/claw-cxx-ft.bindings.system")

(mk:defsystem :gficl/load/ft2
  :source-pathname *gficl-source-dir*
  :binary-pathname *gficl-binary-dir*
  :source-extension "lisp"
  :depends-on ("claw-cxx-ft.bindings" "gficl-bt")
  :components ((:module "src/load"
		:components ((:file "freetype2")))))

#+nil
(mk:oos :gficl/load/ft2 :load :compile-during-load t)

